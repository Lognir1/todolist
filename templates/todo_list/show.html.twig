{% extends 'base.html.twig' %}

{% block title %}TodoList{% endblock %}

{% block body %}
    <h1>TodoList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ todo_list.id }}</td>
            </tr> 
            <tr>
                <th>Name</th>
                <td>{{ todo_list.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ todo_list.description }}</td>
            </tr>
        </tbody>
    </table>

    <h1>Taches associées</h1>
     <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Description</th>
            </tr>
            <tr>
                {% for task in tasks %}
                    <td>{{ task.id }}</td>
                    <td>{{ task.description }}</td>
                {% endfor %}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_todo_list_index') }}">back to list</a>

    <a href="{{ path('app_todo_list_edit', {'id': todo_list.id}) }}">edit</a>

    <a href="{{ path('app_task_new',{'todolist_id': todo_list.id}) }}">Create Task</a>

    {{ include('todo_list/_delete_form.html.twig') }}
{% endblock %}
